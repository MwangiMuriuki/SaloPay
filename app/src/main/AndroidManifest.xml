<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dev.salopay">

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.SaloPay">
        <activity
            android:name=".ActivitySetPassword"
            android:configChanges="orientation|keyboardHidden|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:exported="true"
            android:resizeableActivity="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.SaloPay.NoActionBar"
            android:windowSoftInputMode="adjustResize|stateHidden"
            tools:ignore="LockedOrientationActivity"/>
        <activity
            android:name=".ActivityGetStarted"
            android:configChanges="orientation|keyboardHidden|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:exported="true"
            android:resizeableActivity="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.SaloPay.NoActionBar"
            android:windowSoftInputMode="adjustResize|stateHidden"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ActivityOTP"
            android:configChanges="orientation|keyboardHidden|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:exported="true"
            android:resizeableActivity="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.SaloPay.NoActionBar"
            android:windowSoftInputMode="adjustResize|stateHidden"
            tools:ignore="LockedOrientationActivity"/>

        <activity
            android:name=".ActivityLogin"
            android:configChanges="orientation|keyboardHidden|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:exported="true"
            android:resizeableActivity="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.SaloPay.NoActionBar"
            android:windowSoftInputMode="adjustResize|stateHidden"
            tools:ignore="LockedOrientationActivity"/>

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:exported="true"
            android:resizeableActivity="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.SaloPay.NoActionBar"
            android:windowSoftInputMode="adjustResize|stateHidden"
            tools:ignore="LockedOrientationActivity"/>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>